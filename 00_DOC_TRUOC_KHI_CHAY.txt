HUB-PTN V2.2.3 — SỬA PHẢN HỒI R&D + AUDIT KHÔNG CẦN COMPOSITE INDEX

Bộ dựng dùng đúng ZIP HUB V2.1.1 đang chạy production làm nền.
Không sửa branch main. Không tự đổi Production. Không tự deploy Firestore Rules trong bước build/Preview.

LỖI ĐÃ SỬA TỪ ẢNH TEST
1. R&D bấm “Gửi phản hồi có truy vết” báo:
   Missing or insufficient permissions.
2. Audit trail báo:
   The query requires an index / create_composite...

V2.2.3 XỬ LÝ
- Phản hồi R&D ghi thêm bộ field định danh tương thích cả schema cũ và mới:
  userUid/userEmail + createdByUid/createdByEmail.
- Rules candidate V2.2.3 có quyền ghi rõ cho:
  + department_response của Trưởng phòng R&D có permission rnd.response.manage;
  + interdept_chat theo vai trò được phép;
  + case_attachment theo phạm vi vận hành.
- Private handling KHÔNG được mở rộng.
- Audit trail không còn phụ thuộc query where(caseId)+orderBy(createdAt).
  V2.2.3 query chỉ theo caseId rồi sort trên trình duyệt, vì vậy không cần tạo composite index chỉ để xem lịch sử VM.
- Giữ nguyên chat cố định + thread VM của V2.2.2.
- Giữ nguyên file/ảnh/video/link trong báo cáo và phản hồi.

CÁCH CHẠY
1. Tải ZIP branch hub-v22-builder và giải nén mới.
2. Giữ HUB_PTN_V2_1_1_PRODUCTION_RULES_AND_ONBOARDING*.zip trong Downloads hoặc cạnh bộ dựng.
3. Chạy duy nhất:
   01_BUILD_AND_PREVIEW_HUB_V2_2_3.cmd
4. Script tạo:
   HUB_PTN_V2_2_3_RND_FIX_PREVIEW.zip
   và deploy Preview channel:
   hub-v223-interdept

TEST TRƯỚC RULES
- Mở Preview bằng tài khoản Nguyễn Ngọc Sơn.
- Mở VM-011.
- Audit không được còn link “The query requires an index”.
- Thử gửi phản hồi R&D.

NẾU PHẢN HỒI VẪN BÁO PERMISSION DENIED
Trong ZIP đầu ra chạy:
   02_DEPLOY_RULES_V2_2_3_RND_RESPONSE_TEST.cmd
Sau đó test lại ngay:
- phản hồi R&D;
- ảnh/file đính kèm;
- chat VM-011;
- Trao đổi chung PTN ↔ R&D;
- Owner đọc phản hồi và xác nhận/yêu cầu bổ sung.

Nếu Rules V2.2.3 gây lỗi quyền ngoài dự kiến:
   03_ROLLBACK_RULES_V2_1.cmd

CHƯA CÔNG BỐ PRODUCTION V2.2.3 Ở BƯỚC NÀY.
